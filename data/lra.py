"""ListOps + retrieval (long-range) data

Subsystem data. STAGE 0 STUB — importable placeholder only; the real
implementation arrives in its designated stage (see CLAUDE.md §5).
"""

from __future__ import annotations

STAGE0_STUB = True  


def _not_ready() -> str:
    return "lra: stub (subsystem data); implemented in a later stage."


if __name__ == "__main__":
    
    print(_not_ready())
